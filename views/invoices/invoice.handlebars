 <div class="main-section">              
    <div class="top-bar">
        <div class="top-bar--title">
        </div>
        <ul class="top-bar--nav">
            
        </ul>
    </div>

    <div class="main-content">
        <div class="data-section">
            <div class="material-table">
                <div class="table-header">
                    <span class="table-title">Invoices</span>
                    
                    <div class="actions">
                    <a href="/invoices/add" class="btn-flat nopadding"><i class="material-icons" style="color: #26a69a">add_circle</i><span class="new badge"style="margin-top: 7px;">add </span></a>
                    <a href="#" class="search-toggle waves-effect btn-flat nopadding"><i class="material-icons">search</i></a>
                    </div>
                </div>
                <table class="striped" id="datatable">
                    <thead>
                        <tr>
                            <th class="">Invoice Number</th>
                            <th>Invoice Date</th>
                            <th>Customer</th>
                            <th>Covered By</th>
                            <th>Invoice Total</th>
                            <th>PDF</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        
                        {{#each invoice}}
                        <tr>
                            <p>{{Invoice.dataValues.branch}}</p>
                            <td>MD/{{dataValues.id}}/2019</td>
                            <td>{{dataValues.date}}</td>
                            <td>{{dataValues.customername}}</td>
                            <td><span class="new badge blue" data-badge-caption="">{{dataValues.paymentType}}</span></td>
                            <td>{{dataValues.total}}</td>
                            <td><a href="/invoices/{{dataValues.id}}/pdf" target="_blank" ><i class="material-icons">print</i> </a></td>
                            <td>
                                <a href="/invoices/edit/{{dataValues.id}}" class="button"><i class="material-icons">edit</i></a>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div> 